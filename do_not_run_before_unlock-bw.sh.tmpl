#!/bin/bash
if ! type bw &> /dev/null; then
    echo "bw is not installed, skipping"
    exit 0
fi

if [ -z "$BW_SESSION" ]; then
    echo "No BW_SESSION found, logging in"
    bw login
    export BW_SESSION=$(bw unlock --raw)
fi
